import styled from "styled-components";

export const StyledBankFilter = styled.nav`
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  .selected {
    color: white;
    background-color: var(--color-primary);
  }
`;
